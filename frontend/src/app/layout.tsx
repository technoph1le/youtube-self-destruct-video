import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "💥 Self-Destructing YouTube Video",
  description:
    "The video will be deleted when I reach 4,000 watch hours for my YouTube channel. Why 4,000? Because that's when I get monetized, haha.",
  authors: [
    {
      name: "Technophile",
      url: "https://www.youtube.com/@technoph1le",
    },
  ],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={`${geistSans.variable} ${geistMono.variable}`}>
        {children}
      </body>
    </html>
  );
}
